<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorOutputAreaBackground</item>
        <item name="colorPrimaryDark">@color/colorOutputAreaBackground</item>
        <item name="colorAccent">@color/colorOutputAreaBackground</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

</resources>
